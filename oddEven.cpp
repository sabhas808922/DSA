#include <iostream>
using namespace std;

int main(){
    int n;
    cout << "enter the no. : ";
    cin >> n;

    //using if else conditional statements check whether the entered no. is odd or even 
    /*
    if (n%2 == 0){
        cout << "the no. is even";
    }else{

        cout << "the no.is odd";
    }
    */
    

    // using ternary operator check if the entered no. is odd or even
    // cout << (n%2 == 0 ? "even":"odd");

    return 0;

}

